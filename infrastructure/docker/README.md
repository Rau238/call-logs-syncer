# Optional: Docker PostgreSQL

Docker is **not required**. Use local PostgreSQL + pgAdmin 4 instead.

See: [docs/SETUP-POSTGRES-PGADMIN.md](../../docs/SETUP-POSTGRES-PGADMIN.md)

If you prefer Docker:

```bash
docker compose -f infrastructure/docker/docker-compose.yml up -d
```
