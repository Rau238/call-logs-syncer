# Vite dev uses proxy to localhost:3000 — no VITE_API_URL needed locally.
# For standalone dashboard build against ngrok:
VITE_API_URL=https://ninth-rebalance-deny.ngrok-free.dev/api/v1

# Local combined server (API serves dashboard build):
# Dashboard at https://ninth-rebalance-deny.ngrok-free.dev/ (same origin as API)
